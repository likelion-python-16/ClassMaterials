```python
from dotenv import load_dotenv
# .env 파일에 정의한 환경변수를 불러오는 함수
load_dotenv()
```
- `dotenv` 라이브러리를 써서 프로젝트 최상단에 있는 `.env` 파일을 읽고,
- 그 안에 저장된 `DISCORD_TOKEN` 같은 환경변수들을 **파이썬의 `os.environ`** 에 등록해 줍니다.
---
```python
import os
import re
import discord
from scraper import MusinsaAPI  
# MusinsaAPI: 우리가 직접 구현한 웹 크롤러 클래스
```
- **`os`**: 운영체제 관련 기능을 쓰기 위한 표준 모듈 (`os.getenv()` 로 환경변수 꺼낼 때 사용)
- **`re`**: 정규표현식 라이브러리 (`re.match`/`re.search` 로 메시지 패턴을 찾을 때 사용)
- **`discord`**: Discord 봇 API 라이브러리(`discord.py`)
- **`MusinsaAPI`**: `scraper.py` 에 정의한 **웹 크롤러 클래스**. Musinsa 상품 데이터를 가져오는 역할
---
```python
# ─── Discord 봇 기본 설정 ─────────────────────────────────────────────────
intents = discord.Intents.default()
intents.message_content = True  # 메시지 내용을 읽어오기 위한 권한 설정
client = discord.Client(intents=intents)  # Discord 클라이언트 인스턴스 생성
```
- `Intents`
    - Discord가 어떤 종류의 이벤트(메시지, 반응, 접속 등)를 봇에 전달할지 설정하는 객체
    - `message_content=True` 를 켜야 채팅 내용(`message.content`)을 읽어올 수 있어요.
- `Client`
    - Discord와 연결해 주고, 이벤트(메시지 수신·봇 준비 완료 등)를 처리할 봇 객체를 만듭니다.
---
```python
# ─── Embed 메시지 생성 헬퍼 함수 (개발자 작성) ────────────────────────────────────
def build_message(item):
    # discord.Embed: 디스코드에 이쁘게 보낼 임베드 메시지 객체 생성 (라이브러리 제공)
    embed = discord.Embed(type="rich", title=item["name"], url=item["url"])
    # set_thumbnail: 임베드에 이미지 축소판을 붙여주는 라이브러리 함수
    embed.set_thumbnail(url=item["image"])
    # description: Embed 객체의 본문 영역에 브랜드명 추가 (직접 작성)
    embed.description = item["brand"]
    # add_field: 필드를 추가해 정가/할인가를 표처럼 표시 (라이브러리 함수)
    embed.add_field(name="정가", value=item["originalPrice"], inline=True)
    embed.add_field(name="할인가", value=item["salePrice"], inline=True)
    return embed
```
- 임베드(Embed): 카드 형태로 이미지·제목·필드 등을 깔끔하게 보여주는 Discord 메시지 포맷
- `build_message(item)` 에서:
    1. `Embed(...)` 로 객체 만들고
    2. `set_thumbnail()` 으로 작은 이미지 추가
    3. `description` 에 브랜드명 표시
    4. `add_field()` 로 “정가”/“할인가”를 두 개의 칸으로 표시
- 이렇게 완성된 `embed` 를 돌려주면, `message.channel.send(embed=embed)` 만으로 이쁘게 전송됩니다.
---
```python
# ─── 봇 준비 완료 이벤트 핸들러 (Discord 라이브러리) ─────────────────────────────
@client.event
async def on_ready():
    print("▶ Running file:", __file__)
    # 봇이 로그인해서 준비되면 한 번만 실행됨
    print(f"Logged in as {client.user}")  # 개발자가 디버그용으로 출력
```
- `@client.event` 데코레이터로 이 함수가 이벤트 핸들러(callback)임을 표시
- **`on_ready`**: 봇이 Discord 서버에 성공적으로 연결된 직후 한 번만 호출
- `__file__` 은 “지금 실행 중인 파일 경로”를, `client.user` 은 봇 자신의 이름을 출력
---
```python
# 채널별 추천 캐시: { channel_id: { "keyword": str, "page": int, "items": list } }
last_recommendations: dict[int, dict] = {}
```
`last_recommendations`
- 디스코드의 여러 채널마다 “마지막으로 검색했던 상품 리스트”를 저장해 두는 사전(dict)
- 키는 `channel.id` (숫자), 값은 `{ "keyword": ..., "page": ..., "items": [...] }` 형태
---
```python
# ─── 메시지 수신 이벤트 핸들러 (Discord 라이브러리) ─────────────────────────────
@client.event
async def on_message(message):
    if message.author == client.user:
        return
```
- **`on_message`**: 채널에 메시지가 올 때마다 호출되는 함수
- 제일 먼저 자기 자신(봇)이 보낸 메시지인지 체크 후,  
    자기 메시지면 그냥 `return`(무시)하게 해서 무한반복을 방지
---
```python
    content = message.content.strip()
    content_lower = content.lower()
    channel_id = message.channel.id
    print(f"[디버그] 메시지 수신: {content!r}")
```
- **`.content`**: 메시지 텍스트
- **`.strip()`**: 앞뒤 공백 제거
- **`.lower()`**: 모두 소문자로 바꿔서 “안녕” vs “안녕” 대소문자 구분 없이 처리
- **`message.channel.id`**: 메시지가 온 채널 고유 번호(캐시 키로 사용)
- 디버그 프린트로 콘솔에 찍어서 “정말 이 함수가 호출되고 있는지” 확인
---
1) 신발 추천
```python
if ("신발추천" in content_lower
	or "신발 추천" in content_lower
	or content_lower == "신발"):
	# MusinsaAPI로 검색 → 최대 5개
	last_recommendations[channel_id] = { ... }
	await message.channel.send(f"👟 **신발 추천 TOP5**\n{reply}")
	return
```
- “신발추천”, “신발 추천”, 혹은 “신발” 만 입력해도 버튼
- `MusinsaAPI(keyword="신발", size=5).fetch()` 로 데이터 가져옴
- 캐시에 `{ keyword, page, items }` 저장
- 텍스트 형태로 **TOP5** 상품 나열

2) 반팔 추천
```python
if ("반팔추천" in content_lower
	or "반팔 추천" in content_lower
	or "티셔츠추천" in content_lower
	or "티셔츠 추천" in content_lower):
	# 비슷한 흐름으로 fetch → 캐시 → 출력
```
- “반팔추천” 계열 키워드를 모두 처리

3) 상세정보 보기
```python
m = re.match(r"(\d+)번 상품 상세", content_lower)
if m and channel_id in last_recommendations:
	idx = int(m.group(1)) - 1
	item = last_recommendations[channel_id]["items"][idx]
	await message.channel.send(embed=build_message(item))
	return
```
- `re.match` 로 “숫자+번 상품 상세” 패턴 찾기
- 매치되면 **번호**를 꺼내 캐시된 `items` 리스트에서 해당 상품 선택
- `build_message(item)` 임베드 전송

4) 찜하기
```python
m = re.match(r"(\d+)번 상품 찜", content_lower)
if m and channel_id in last_recommendations:
	# 번호 파싱 → 같은 방식으로 찜 완료 메시지
```
- “n번 상품 찜” 입력 시 간단 텍스트 응답 (“💖 찜했어요!”)

5) 가장 저렴한 제품
```python
if "가장 저렴" in content_lower and channel_id in last_recommendations:
	cheapest = min(items, key=lambda x: int(x["salePrice"].replace("원","").replace(",","")))
	await message.channel.send(f"💸 가장 저렴한 상품:\n{cheapest['name']} – {cheapest['salePrice']}")
	return
```
- 캐시된 상품들 중 `salePrice` 숫자만 비교해 최저가 상품 찾기

6) 다음 페이지
```python
if "다음 페이지" in content_lower and channel_id in last_recommendations:
	cache = last_recommendations[channel_id]
	items = MusinsaAPI(keyword=cache["keyword"], page=cache["page"]+1, size=5).fetch()
	# 캐시 업데이트 → 2페이지 결과 출력
```
- “다음 페이지” 입력 시 이전 `keyword`와 `page` 값을 읽어와 `page+1` 호출

기타 간단 명령
```python
if content_lower in ("안녕하세요", "안녕", "안녕하십니까"):
	await message.channel.send(f"{message.author.display_name}님, 안녕하세요! 😊")
if content_lower.startswith("$hello"):
	await message.channel.send("Hello!")
if content_lower.startswith("$hi") or content_lower == "hi":
	await message.channel.send(f"hi, {message.author.display_name}! 🙂")
if content_lower.startswith("!타임세일"):
	embeds = [build_message(i) for i in MusinsaAPI().fetch()[:10]]
	await message.channel.send(embeds=embeds)
```
- 한국어/영어 인사, `$hello`, `$hi`, `!타임세일` 등 간단한 키워드 반응

```python
# ─── 스크립트 직접 실행 진입
if __name__ == "__main__":
    token = os.getenv("DISCORD_TOKEN")
    if not token:
        raise RuntimeError("❌ DISCORD_TOKEN 환경변수가 설정되지 않았습니다.")
    client.run(token)
```
- 직접 `python bot.py` 로 실행할 때만 이 부분이 돌아가며,
- `.env` 에 저장된 `DISCORD_TOKEN` 을 읽어와 봇을 로그인/실행시킵니다.

전체 흐름 요약
- 환경 변수 로딩 → 라이브러리 임포트 → 클라이언트 생성
- **`on_ready`** 로봇 준비 상태 알림
- **`on_message`** 에서
    - 자기 메시지 무시
    - 채팅 내용 파싱 → 디버그 출력
    - 1~6번 분기로 “추천 → 상세 → 찜 → 최저가 → 페이지” 처리
    - 기타 간단 명령 처리
- 메인 진입점 에서 토큰 확인 후 `client.run()` 으로 봇 가동