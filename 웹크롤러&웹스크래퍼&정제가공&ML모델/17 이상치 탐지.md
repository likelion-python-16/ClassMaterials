이상치(Outlier)란?
	다른 데이터들과 너무 동떨어진 값을 말해요.
EX):
- 평균 나이가 20~70살인데 200살이 있다면? → 이상치!
- 월급이 대부분 200만~500만인데 1억 원이 있다면? → 이상치!

---
이런 값이 있으면 분석 결과나 머신러닝 예측이 엉뚱하게 나올 수 있어요.

---
이런 이상치가 왜 생기는가?
`입력 실수`	나이: 28살 입력하려다 280살 입력 
`측정 오류`	체중 센서가 고장 나서 -15kg 표시
`진짜 특이한 사례`	일반 직원 연봉은 3천만 원인데, CEO 연봉 5억
`단위 착오`	월급 500인데 만원 → 원을 잘못 처리해서 5,000,000

---
왜 이상치가 문제일까?
- 평균 계산을 왜곡시켜요.
- 머신러닝 모델이 이상치에 끌려가서 엉뚱하게 학습해요.
- 시각화할 때 데이터 범위가 너무 커져 트렌드가 안 보일 수도 있어요.
---
이상치를 찾는 대표적인 방법 2가지
1. IQR(Inter-Quartile Range) 방법 (사분위수 기반) : 
	뜻은 "가운데 50%에 해당하는 값들의 범위"입니다.
	박스 플롯(Boxplot)에 쓰이는 방식이에요.
쉽게 말해서
	데이터를 낮은 값부터 높은 값까지 줄 세운 다음,  
	중간 50%가 차지하는 범위를 기준으로  
	너무 작거나 너무 큰 값은 이상치로 간주하는 방법이에요.

핵심 개념:
`Q1` :	 1사분위수 (25% 지점 값)
`Q3` :	 3사분위수 (75% 지점 값)
`IQR` : Q3 - Q1, 중간 50% 범위
단, Q1,Q3,IQR은 단지 변수일 뿐입니다.
`quantile()` : `DataFrame`이나 `Series`에서 특정 위치의 분위 값을 계산해주는 함수예요.
- `Series`은 판다스에서 사용되는 용어로 1차원 데이터 `[25, 30, 35]`를 말하며,
- `DataFrame`은 2차원 데이터로 `표 형식 (행+열)`를 말합니다. 엑셀표를 생각하면 됩니다.

이상치 기준:
	Q1 - 1.5×IQR 보다 작거나,  
	Q3 + 1.5×IQR 보다 큰 값

폰트가 깨져 보일경우:
```
sudo apt update
sudo apt install fonts-nanum
fc-list | grep "Nanum"
```

예상출력결과:
```
/usr/share/fonts/truetype/nanum/NanumGothic.ttf: NanumGothic:style=Regular
```

```bash
sudo fc-cache -fv
rm -rf ~/.cache/matplotlib
```
matplotlib은 내부적으로 폰트 정보를 캐싱하는데, 이걸 지워줘야 새 폰트를 인식합니다. 그리고 Jupyter 또는 Python 다시 실행

```python
df = pd.read_csv("csv_files/combined_customers.csv")

# Q1, Q3 계산
Q1 = df['age'].quantile(0.25)
Q3 = df['age'].quantile(0.75)
IQR = Q3 - Q1

# 이상치 조건
outliers = df[(df['age'] < Q1 - 1.5 * IQR) | (df['age'] > Q3 + 1.5 * IQR)] # 나이가 너무 작거나, 너무 큰 경우를 이상치로 본다
# Q1 - 1.5×IQR ~ Q3 + 1.5×IQR 사이

# Q1: 아래쪽 25% 지점, Q3: 위쪽 75% 지점
# IQR = Q3 - Q1 즉, 가운데 50% 데이터 범위 
# df['age'] < Q1 - 1.5 * IQR : 나이가 매우 낮아서 일반적인 범위보다 한참 아래인 경우
# df['age'] > Q3 + 1.5 * IQR : 나이가 매우 많아서 일반적인 범위를 훨씬 초과하는 경우

# 박스 플롯 시각화
import matplotlib.pyplot as plt
import matplotlib.font_manager as fm
import seaborn as sns

# 정확한 경로로 폰트 지정
font_path = '/usr/share/fonts/truetype/nanum/NanumGothic.ttf'
fontprop = fm.FontProperties(fname=font_path)
print("폰트 이름:", fontprop.get_name())

# 폰트 설정은 matplotlib 전체에 적용하지 않고, plot마다 개별 적용
plt.figure(figsize=(6, 8))
sns.boxplot(y='age', data=df, width=0.3, showfliers=True)
sns.stripplot(y='age', data=df, color='red', alpha=0.5, jitter=True)

# 폰트를 직접 넘김 (핵심!)
plt.title("나이(age) 분포 - 이상치 포함", fontsize=14, fontproperties=fontprop)
plt.ylabel("나이 (age)", fontproperties=fontprop)
plt.xlabel("", fontproperties=fontprop)
plt.tight_layout()
plt.show()
```
박스 플롯에서 동그란 점으로 표시되는 것들이 이상치입니다.

- `Q1 - 1.5 * IQR`: 너무 작은 값 (이 범위 아래는 이상치)
- `Q3 + 1.5 * IQR`: 너무 큰 값 (이 범위 위는 이상치)

![[Pasted image 20250716085016.png]]

---
`Q1 = df['age'].quantile(0.25)`
> 나이(age) 데이터를 작은 값부터 큰 값까지 정렬했을 때, 아래쪽 25% 지점의 값을 구합니다.  이걸 제1사분위수 (Q1) 라고 합니다. 
> 예: 나이 순서대로 줄 세웠을 때, 하위 25% 지점의 나이

`Q3 = df['age'].quantile(0.75)`
> 나이 데이터에서 위쪽 75% 지점의 값, 즉 제3사분위수 (Q3) 를 구해요.  
> 예: 나이 순서대로 줄 세웠을 때, 상위 25%가 시작되는 나이

`IQR = Q3 - Q1`
> IQR (Interquartile Range) 는  
> Q1과 Q3 사이의 값의 범위(=중간 50%) 를 의미해요.  
> 즉, 대부분 사람들이 있는 '정상적인 범위' 라고 보면 돼요.
> 사용 예: 가격, 소득, 나이 분포 등
---
2. Z-score 방법 (평균 기반)
	평균(Mean) 과 표준편차(Standard Deviation) 를 기준으로 판단하는 방법

핵심 개념:
- Z-score는 "이 값이 평균에서 얼마나 떨어졌는지" 를 나타내는 지표예요.
- 보통 Z-score가 +3 이상 또는 -3 이하이면 "너무 멀리 있다" → 이상치로 간주

예시:
- 평균 나이가 40이고, 표준편차가 10이라면  
    → 나이 70은 (70 - 40) / 10 = Z-score 3.0 → 이상치!  
    → 나이 10도 Z-score -3.0 → 이상치!
표준편차 구하기: 
```
# 나이데이터
ages = [20, 22, 24, 26, 28]

# 평균값은 
(20+22+24+26+28)/5 = 24

# 각 값에서 평균을 뺀 차이 구하기 (편차)
[20-24, 22-24, 24-24, 26-24, 28-24] → [-4, -2, 0, 2, 4]

# 편차 제곱
[(-4)^2, (-2)^2, 0^2, 2^2, 4^2] → [16, 4, 0, 4, 16]

# 제곱값 평균
(16+4+0+4+16​)/5 = 5

편차 제곱 = 40

모분산 40/5 = 8

표본 분산 40/4 = 10 

모표준편차 = 

# 제곱값 평균의 루트 = 표준편차
모표준편차 √8 = 2.83
즉 이 데이터의 표준 편차는 약 2.83입니다.

표본표준편차:
√10 = 3.16
```

pandas공식으로 표준편차 구하기:
```python
import pandas as pd

ages = [20, 22, 24, 26, 28]
df = pd.DataFrame({'age': ages})

std_population = df['age'].std(ddof=0)
print("표준편차:", std_population)
```
---
Z-Score 기반 이상치 탐지
(정규분포 가정 / 평균과 표준편차 기준)
```python
# 넘파이방식
import numpy as np
df = pd.read_csv("csv_files/combined_customers.csv")

# 평균과 표준편차
mean_age = np.mean(df['age'])
std_age = np.std(df['age'])

# Z-score 계산
df['z_score'] = (df['age'] - mean_age) / std_age

# Z-score가 ±3을 넘으면 이상치
outliers = df[np.abs(df['z_score']) > 3]
```

`df['age']`	개별 사람의 나이 값들 (예: 20, 25, 30...)
`mean_age`	전체 평균 나이 (예: 26.5)
`std_age`	나이의 표준편차 (예: 4.2 정도)
`z_score`	평균에서 얼마나 떨어져 있는지를 표준편차 기준으로 측정한 수치
`std` → Standard Deviation (표준편차)
`abs` → Absolute Value (절댓값)
```
print(abs(-5))   # 출력: 5
print(abs(3.2))  # 출력: 3.2
```

- Z-score는 각 값이 평균에서 얼마나 떨어져 있는지 측정
- Z-score가 +3 또는 -3을 넘는 데이터는 이상치로 간주
- 데이터가 정규분포일 때 효과적
- 사용 예: 센서 데이터, 시험 점수 등

```python
# 판다스 방식
import pandas as pd

# 데이터 불러오기
df = pd.read_csv("csv_files/combined_customers.csv")

# 평균과 표준편차 (Pandas 메서드 사용)
mean_age = df['age'].mean()
std_age = df['age'].std(ddof=0)  # NumPy의 np.std와 일치시키려면 ddof=0

# Z-score 계산
df['z_score'] = (df['age'] - mean_age) / std_age

# Z-score가 ±3을 넘는 이상치 추출
outliers = df[df['z_score'].abs() > 3]
```

NumPy란?
Pandas는 내부적으로 NumPy를 기반으로 만들어진 라이브러리입니다.
즉, Pandas 안에 NumPy가 들어가 있고, 실제 데이터도 NumPy 배열(`ndarray`) 형태로 저장됩니다. 둘다 데이터 분석을 하기 위한 중요한 라이브러리이지만 역할과 목적이 다릅니다.

NumPy는 숫자 배열 계산이고(고속 수치 연산)
Pandas는 표 형태 데이터 처리 (엑셀처럼 행-열구조)

---
IQR 방식과 Z-score 방식은 모두 이상치(Outlire)를 탐지하는 기법이지만 기준과 강점이 다릅니다.

핵심차이 요약: 
IQR 방식
- 기준: 사분위수(Q1, Q3) 기반
- 분포가정: 비정규 분포에도 강함
- 이상치 정의: Q1 - 1.5×IQR 보다 작거나, Q3 + 1.5×IQR 보다 큰 값
- 민감도: 극단값에 덜 민감 (중앙값 중심)
- 시계열/비대칭에 강함

Z-score 방식
 - 기준: 평균, 표준편차 기반
- 분포가정: 정규분포일 때 적합
- 이상치 정의: 평균에서 ±3 표준편차 이상 떨어진 값
- 민감도: 극단값에 민감 (평균이 흔들릴 수 있음)
- 시계열/비대칭에 약함함