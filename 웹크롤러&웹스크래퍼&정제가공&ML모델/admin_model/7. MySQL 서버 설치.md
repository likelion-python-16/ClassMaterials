1. MySQL 서버 설치 & 기초 설정
WSL(Ubuntu)
```bash
sudo apt update
sudo apt install -y mysql-server mysql-client
```
macOS (Homebrew)
```bash
# Homebrew 업데이트
brew update

# MySQL 설치
brew install mysql

# 부팅 시 자동 시작 설정 (선택)
brew services start mysql
```

MySQL 서비스 시작
WSL(Ubuntu)
```bash
sudo service mysql start
```
macOS
```bash
# 실행 중인지 확인
brew services list

# 시작
brew services start mysql
```
---

2. 초기 보안 설정 실행: 명령어 같음
```bash
sudo mysql_secure_installation
```
- 비밀번호 복잡도 정책 선택
	– 비밀번호의 최소 강도(정책)를 정하는 단계입니다.  
	– 0: 검증 안 함, 1: 보통, 2: 강력  `1`
- Change the root password? [Y/n]  `y`
    – 루트(root) 계정의 비밀번호를 변경하시겠습니까?   
- Remove anonymous users? [Y/n]  `y`
    – 익명(anonymous) 사용자를 제거하시겠습니까?  
    (비밀번호 없이 접속 가능한 기본 테스트 계정)
- Disallow root login remotely? [Y/n]  `y`
    – 원격(reymote)에서의 root 로그인(접속)을 금지하시겠습니까?
- Remove test database and access to it? [Y/n] ` y`
    – test라는 이름의 기본 테스트 데이터베이스와 그 접근 권한을 제거하시겠습니까?
- Reload privilege tables now? [Y/n]  `y`
    – 변경된 권한 테이블을 즉시 다시 불러오시겠습니까? (지금 바로 반영)
    
---
3. 루트 계정 비밀번호 설정
```bash
sudo mysql 
```

루트 계정에 비밀번호 설정 (처음 한 번만 실행)
```bash
ALTER USER 'root'@'localhost'
  IDENTIFIED WITH mysql_native_password
  BY 'DjangoUserPass!123';
FLUSH PRIVILEGES;
```

---
4. Django용 DB 및 사용자 생성 (1회만)
```bash
-- 데이터베이스 생성
CREATE DATABASE restaurant_db CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;

-- 사용자 생성
CREATE USER 'restaurant_user'@'localhost' IDENTIFIED BY 'DjangoUserPass!123';

-- 권한 부여
GRANT ALL PRIVILEGES ON restaurant_db.* TO 'restaurant_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

WSL(Ubuntu) 터미널에서 MySQL 설정 파일 열기
```bash
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf
```

맨 위에 있는 `bind-address` 설정을 찾으세요. 기본은 `127.0.0.1` 일 텐데,
```bash
bind-address    = 127.0.0.1
# 위의 내용을 아래와 같이 수정한다.
bind-address    = 0.0.0.0

# 그리고 Mysql재시작
sudo service mysql restart

# 연결 확인
sudo netstat -tlnp | grep 3306

# 출력에 `0.0.0.0:3306` 이 보이면 OK.

저장하고 빠져나옴
```

DB / 사용자 최종 확인
```bash
mysql -u root -p
```

`restaurant_user`가 `restaurant_db`에 권한이 있는지 꼭 확인하세요.
```python
SHOW DATABASES;
SELECT user, host FROM mysql.user;
SHOW GRANTS FOR 'restaurant_user'@'localhost';
```

`.env` 파일 설정 (Django 프로젝트 내부)
```python
DB_NAME=restaurant_db
DB_USER=restaurant_user
DB_PASSWORD=DjangoUserPass!123
DB_HOST=127.0.0.1
DB_PORT=3306
```

`settings.py` 연결
```python
import os
from dotenv import load_dotenv

# .env 파일 불러오기
load_dotenv()

# 환경변수 읽기
DB_NAME = os.environ.get("DB_NAME")
DB_USER = os.environ.get("DB_USER")

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": os.environ.get("DB_NAME"),
        "USER": os.environ.get("DB_USER"),
        "PASSWORD": os.environ.get("DB_PASSWORD"),
        "HOST": os.environ.get("DB_HOST", "127.0.0.1"),
        "PORT": os.environ.get("DB_PORT", "3306"),
        "OPTIONS": {
            "charset": "utf8mb4",
            "init_command": "SET NAMES utf8mb4",
        },
    }
}
```

---
최종 확인 & 환경 변수
```bash
mysql -u root -p
SELECT user, host FROM mysql.user;
SHOW DATABASES;
```

```
mysql> SELECT user, host FROM mysql.user;
+------------------+-----------+
| user             | host      |
+------------------+-----------+
| debian-sys-maint | localhost |
| django_user      | localhost |
| mysql.infoschema | localhost |
| mysql.session    | localhost |
| mysql.sys        | localhost |
| restaurant_user  | localhost |
| root             | localhost |
+------------------+-----------+


mysql> SHOW DATABASES;
+--------------------+
| Database           |
+--------------------+
| information_schema |
| myproject_db       |
| mysql              |
| performance_schema |
| restaurant_db      |
| sys                |
+--------------------+
```

---
Python-MySQL 드라이버 설치
```bash
pip install mysqlclient
```
만약 `mysqlclient` 빌드 오류 발생 시
```bash
sudo apt install -y default-libmysqlclient-dev build-essential
pip install mysqlclient
```

---
WSL은 윈도우에서 DBever를 실행합니다.
DBever설치 링크[https://dbeaver.io/download/](https://dbeaver.io/download/)

---
설치하기
![[Pasted image 20250712222734.png]]

DBeaver와 MySQL과 연결
![[Pasted image 20250712160842.png]]

![[Pasted image 20250712160913.png]]

왼쪽에서 `restaurant_db` → 오른쪽 클릭 → Edit Connection...
![[Pasted image 20250712165724.png]]

터미널에서 DBeaver 실행
WSL(Ubuntu)
```bash
# MySQL 시작
sudo service mysql start

# 상태 확인
sudo service mysql status

# MySQL 중지
sudo service mysql stop
```

MYSOL과 연결하기
![[Pasted image 20250712214814.png]]


상단 탭에서 `Driver Properties` 클릭
필터에 `allowPublicKeyRetrieval` 검색
![[Pasted image 20250712165941.png]]
- **Key**: `allowPublicKeyRetrieval`
- **Value**: `true`

`useSSL = false` 설정 추가
![[Pasted image 20250712170200.png]]

전체 테이블 다이어그램 보기:
![[Pasted image 20250712175827.png]]

![[Pasted image 20250712182016.png]]

한글로 인터페이스를 변경하기
![[Pasted image 20250712223609.png]]

![[Pasted image 20250712223452.png]]